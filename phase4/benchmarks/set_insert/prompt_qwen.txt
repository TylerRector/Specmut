You are generating compact Lean 4 theorem statements for an existing benchmark file.

A scaffold has ALREADY been written above your output. The scaffold declares:

    def Distinct (l : List Nat) : Prop := ...
    axiom setInsert : Nat → List Nat → List Nat

Your output is concatenated to that scaffold to form a single Lean file.

OUTPUT RULES (HARD):
- Return ONLY raw Lean source. No markdown fences. No prose.
- Do NOT include any imports.
- Do NOT use any of: def, inductive, axiom, lemma, example, match, by, begin, end, namespace, open, import, class, structure, instance, abbrev, notation, #check, #eval, #print.
- Do NOT redefine setInsert or Distinct.
- Do NOT introduce helper Bool functions such as isMember or hasDuplicate;
  the previous run showed that helpers like these compile but disconnect
  the spec from the mutation target and yield tau = 0.
- Produce EXACTLY 2 theorem declarations.
- Each theorem MUST end with exactly `:= sorry`.
- Keep your output under 30 lines.

ALLOWED IDENTIFIERS:
- Nat
- List
- setInsert         (declared above as axiom)
- Distinct          (declared above as predicate)
- ∈, ≠, =
- ∀, ∃, ∧, ∨, →, ¬
- ::, []

OUTPUT GRAMMAR:

    theorem <name_1> : <proposition> := sorry

    theorem <name_2> : <proposition> := sorry

ACCEPTABLE SHAPE EXAMPLES (do not copy verbatim; pick strong constraints,
not tautologies — weak theorems were the dominant failure last run):

    theorem setInsert_preserves_distinct_generated (k : Nat) (xs : List Nat) :
        Distinct xs → Distinct (setInsert k xs) := sorry

    theorem setInsert_idempotent_distinct_generated (k : Nat) (xs : List Nat) :
        Distinct (setInsert k (setInsert k xs)) := sorry

Avoid theorems whose proposition is trivially true (e.g. `x = x`, or
membership claims that hold for every function regardless of behavior).

Now produce exactly 2 theorem declarations about setInsert using only the
allowed identifiers. Begin your output with `theorem ` on the first line.
