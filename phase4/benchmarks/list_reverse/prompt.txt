Write a Lean 4 specification for reversing a list of natural numbers.

Requirements:
- Declare `rev : List Nat → List Nat` as an `axiom`.
- Define helper predicates as `def` with concrete recursive bodies.
- State theorems (using `:= sorry`) that fully specify list reversal.

The specification should be self-contained (no imports beyond Lean's core).

Properties that a strong specification should capture:
- `rev` is an involution: rev (rev xs) = xs.
- `rev` preserves length.
- `rev` preserves membership (same elements in either direction).

Output ONLY the Lean code.  Do not include explanations or commentary.
